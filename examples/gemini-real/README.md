# Gemini Real Example

This example uses the official Google GenAI Go SDK and performs a simple function call loop.

## Setup
- Set `GOOGLE_API_KEY` in your environment.

## Run
```
cd examples/gemini-real

go run .
```
