# Anthropic Real Example

This example uses the official Anthropic Go SDK and performs a simple tool call loop.

## Setup
- Set `ANTHROPIC_API_KEY` in your environment.

## Run
```
cd examples/anthropic-real

go run .
```
